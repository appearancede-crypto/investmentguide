# Upper bounds cap major versions to avoid silent breaking upgrades.
# pandas>=2.1 is required for Styler.map (used by the dashboard).
# requests>=2.32 excludes the CVE-2023-32681 / CVE-2024-35195 range.
pandas>=2.1,<3
numpy>=1.24,<3
requests>=2.32,<3
streamlit>=1.30,<2
plotly>=5.18,<7
PyYAML>=6.0,<7
matplotlib>=3.5,<4   # required by pandas Styler.background_gradient in the dashboard
pytest>=7.0
