# Minimal dependencies for the hosted Signal Engine web server.
# (The Streamlit dashboard's heavier deps — streamlit, plotly, matplotlib — are
#  NOT needed here; the web UI renders in the browser from JSON.)
pandas>=2.1,<3
numpy>=1.24,<3
requests>=2.32,<3
PyYAML>=6.0,<7
